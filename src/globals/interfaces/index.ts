import * as Middleware from "./middlewares.interface";
import * as Controller from "./controllers.interface";
import * as JSONResponse from "./response.interface";

export { Middleware, Controller, JSONResponse };
